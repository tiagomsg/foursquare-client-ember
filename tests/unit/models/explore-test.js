import { moduleFor, test } from 'ember-qunit';

moduleFor('model:explore', 'Unit | Model | explore');

test('it exists', function(assert) {
  let explore = this.subject();
  assert.ok(!!explore);
});
